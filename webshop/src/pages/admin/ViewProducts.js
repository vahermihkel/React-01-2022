
function ViewProducts() {
  return (<div>ViewProducts / otsing -- otsida ID / nime alusel
    onKeyUp() -- iga klahvivajutusega toimub otsing
    // 2 - useState()
    originaali jaoks, teine on väljanäidatava jaoks
    KÕIK TOOTED - 100

    VÄLJA NÄIDATAKSE 10

    KÕIK TOOTED - võtan teise useState 
  </div>)
}

export default ViewProducts;